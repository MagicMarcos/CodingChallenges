// create a function that will return an array from 1 to n
[...Array(10).keys()].map(x => x++);
// create another function that returns an array from 0 to n-1
[...Array(10).keys()];
// create a function that adds all numbers from 1 to n
function f(n) {
	return (n * (n + 1)) / 2;
}
